syntax = "proto3";

package Radar;

message Chunk { bytes Content = 1; }

message GenerateReq {
  string user_id = 1;
  string portfolio_id = 2;
}

service ReportGenerator {
  rpc generate(GenerateReq) returns (stream Chunk) {}
}
